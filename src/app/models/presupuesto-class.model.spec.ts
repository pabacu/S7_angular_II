import { PresupuestoClass } from './presupuesto-class.model';

describe('PresupuestoClass', () => {
  it('should create an instance', () => {
    expect(new PresupuestoClass()).toBeTruthy();
  });
});
